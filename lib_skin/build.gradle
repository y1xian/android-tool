ext.isLibrary = true
apply from: rootProject.file('lib.gradle')

dependencies {
    implementation project(':lib_common'), project(':lib_adapter')
    implementation libs.support_v7, libs.recyclerview, libs.design
}
